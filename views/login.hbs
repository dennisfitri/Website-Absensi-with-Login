<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
    <link rel="stylesheet" href="../public/css/style.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins&family=Source+Sans+Pro:wght@200;300&display=swap');


        *{
            padding: 0;
            margin: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body{
            background-color: #B7B7B7;
            background-image: url(absensi.jpg);
        }   

        nav{
            background: #0A4D68;  
            color: #ddd;
            padding: 30px 60px;
            display: flex;
            justify-content: space-between;
            background-attachment: fixed;
            position: fixed;
            width: 100%;
            z-index: 2;
        }

        nav ul{
            display: flex;
            justify-content: space-around;
            align-items: center;
            font-size: 15px;
        }

        nav li{
            list-style: none;
        }

        nav li a{
            text-decoration: none;
            color: #fff;
            font-weight: bold;
            padding: 5px 8px;
        }

        nav li a:hover{
            color: #ddd;
            text-decoration: none;
        }

        .container{
            position: relative;
            width: 500px;
            height: 350px;
            background-color: gray;
            margin: 100px auto;
            color: #ddd;
            text-align: center;
            border-radius: 40px;
            opacity: 75%;
        }

        .container .title{
            position: absolute;
            top: 100px;
            left: 110px;
        }

        .container:hover{
            opacity: 100%;
            transition: 0.8s;
        }

        .container-login{
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20% 0;
            flex-wrap: wrap;
            margin: auto;
        }

        .container-login .box{
            position: relative;
            width: 450px;
            height: 600px;
            color: #fff;
            background: #111;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 20px 30px;
            transition: 0.5s;
        }

        .container-login .box::before{
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, #03a9f4, #7C96AB);
        }

        .container-login .box::after{
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, #03a9f4, #7C96AB);
        }

        .container-login .box span{
            position: absolute;
            top: 80px;
            left: 6px;
            right: 6px;
            bottom: 6px;
            background: rgba(0, 0, 0, .6);
            z-index: 2;            
        }

        .container-login .box span::before{
            content: '';
            position: absolute;
            top: 0px;
            left: 0px;
            width: 74%;
            height: 100%;
            background: rgba(255, 255, 255, 0.1);
            z-index: 2;
            pointer-events: none;
            filter: blur(25px);
        }

        .container-login .box .content{
            position: relative;
            z-index: 10;
            padding: 20px 40px;
            align-items: center;
        }

        .titlee{
            font-size: 50px;
            color: #fff;
            text-align: center;
            margin-top: -40px;
            margin-bottom: 60px;
        }

        .subtitle{
            color: #fff;
            font-size: 30px;
            text-align: center;
            margin-bottom: 50px;
        }

        .text{
            font-family: 'Poppins', sans-serif;
            font-size: 23px;
            color: #fff;
            text-align: center;
            margin-bottom: 5px;
        }

        .input{
            color: black;
            padding: 10px 20px;
            width: 280px;
            align-items: center;
            margin-bottom: 20px;
            border-radius: 0spx;
            border: 2px solid #111;
            background-color: white;
            font-family: 'Source Sans Pro', sans-serif;
            text-align: center;
            outline: none; 
        }

        .input:focus{
            transform: scale(1.04);
            transition: 0.4s;
        }

        .box:hover{
            transform: scale(1.08);
        }

        .space{
            height: 190px;
        }

        .button{
            display: flex;
            border: 2px solid black;
            border-radius: 10px;
            padding: 5px 20px; 
            font-family: 'Poppins', sans-serif;
            font-size: 20px;
            margin: auto;  
            background-color: white; 
            cursor: pointer;
            outline: none; 
            opacity: 60%;
        }

        .button:hover{ 
            opacity: 100%;
        }

        .button:active{
            transform: scale(1.02);
        }

        .alert{
            color: #ff0050;
            padding: 13px 30px;
            width: auto;
            height: 50px;
            background-color: #bdc3c7;
            border-radius: 40px;
            box-shadow: 0 0 20px 4px #ddd;
        }
    </style>
    <title>Login</title>
</head>
<body>
    <nav>
        <h4>Absensi</h4>
        <ul>
            <li><a href="/login">Login</a></li>
            <li><a href="/register">Register</a></li>
        </ul>
    </nav>

    <div class="container-login">
        <div class="box">
            <span></span>
            <div class="content">
                <form action="/auth/login" method="POST">
                        <h1 class="titlee">Login Form</h1>

                        <label for="email" class="text">Email Address</label>
                        <input type="email" class="input" id="email" name="email">

                        <label for="password" class="text">Password</label>
                        <input type="password" class="input" id="password" name="password">
                        
                        <div class="space"></div>

                    <button type="submit" class="button">Login</button>
                </form>
            </div>
        </div>
        {{#if message}}
            <div class="alert-container">
                <h4 class="alert alert-danger mt-4">{{message}}</h4>
            </div>
        {{/if}}
    </div>

</body>
</html>